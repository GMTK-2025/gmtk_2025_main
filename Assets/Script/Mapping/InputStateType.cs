namespace Mapping.Input
{
    public enum InputStateType
    {
        DISABLE_ENTER,
        DISABLE_EXIT,
        DEFAULT_ENTER,
        DEFAULT_EXIT,
        CONFIRM_ENTER,
        CONFIRM_EXIT,
        SELECT_ENTER,
        SELECT_EXIT,
        CHARACTER_SELECT_ENTER,
        CHARACTER_SELECT_EXIT,
    }
}